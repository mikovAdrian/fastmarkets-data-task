name: fastmarkets
version: "1.0.0"
config-version: 2

profile: fastmarkets

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  fastmarkets:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
